const express = require("express"); const path = require("path"); const app = express(); app.use(express.json()); let points = 100; app.get("/", (req, res) => res.sendFile(path.join(__dirname, "index.html"))); app.post("/api/add", (req, res) => { points += 50; res.json({ points }); }); app.post("/api/withdraw", (req, res) => { if(points < 1000) return res.json({ error: "رصيدك غير كافٍ للسحب" }); points -= 1000; res.json({ points }); }); app.listen(3000, () => console.log("Server is running on port 3000..."));
